{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "description": "Owns the queues. Will generate segments ahead of playback: Claude plans → Spotify search resolves tracks → ElevenLabs renders the commentary → bucket. Today: boots pg-boss, proves Postgres + Spotify search, waits.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@radio/db": "workspace:*",
    "@radio/spotify": "workspace:*",
    "pg-boss": "^12.27.0",
    "tsx": "^4.23.12",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.9.3"
  }
}

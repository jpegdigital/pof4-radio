# Keeps every editor and tool writing what Biome and .gitattributes expect.
# Biome 2 reads this file too (formatter.useEditorconfig defaults to true).
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

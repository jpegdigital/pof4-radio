{
  "name": "@radio/spotify",
  "version": "0.0.0",
  "private": true,
  "description": "Spotify Web API, the two flows we use: client credentials (search/lookup — the worker) and the user's authorization code grant (playback — the web app). Pure fetch; config is passed in, never read from env.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.9.3"
  }
}

{
  "name": "@radio/dj",
  "version": "0.0.0",
  "private": true,
  "description": "The producer's pure logic: the shapes of a record, a card, a line and a kept segment; the clock rules; timings from an alignment; the assembly ladder; the prompt slots and tool schemas. Nothing here reads the environment or calls a service.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.9.3"
  }
}
